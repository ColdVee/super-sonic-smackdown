function onEvent(n,v1,v2)


	if n == 'SetY' then
		yNew = tonumber(v2)
		setProperty(v1..'.y', yNew);
	end



end

function onTimerCompleted(tag, loops, loopsLeft)
	if tag == 'explosionEnd' then
		removeLuaSprite('explosion', true)
	end
end